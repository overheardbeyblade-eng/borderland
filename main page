<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ゲーム開始</title>
<style>
  body {
    background-color: #fff;
    color: #000;
    font-family: 'Courier New', monospace;
    text-align: center;
    padding-top: 25vh;
  }

  h1, p {
    margin: 8px 0;
    font-weight: normal;
  }

  #countdown {
    font-size: 1.2em;
    margin: 20px 0;
  }

  .button-container {
    margin-top: 40px;
  }

  button {
    font-size: 1em;
    padding: 10px 25px;
    margin: 10px;
    background: black;
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    transition: 0.3s;
  }

  button:hover {
    background: #444;
  }
</style>
</head>
<body>

  <p>ようこそ</p>
  <p>プレイヤー め ゲームへ</p>
  <p>まもなく</p>
  <h1>【GAME】</h1>
  <p>開始します</p>

  <div id="countdown"></div>

  <div class="button-container">
    <button onclick="window.location.href='game1.html'">Game 1</button>
    <button onclick="window.location.href='game2.html'">Game 2</button>
  </div>

<script>
  // Countdown ke 8 November 2025 pukul 15:00 WIB
  const targetDate = new Date("November 8, 2025 15:00:00 GMT+0700").getTime();

  const countdown = setInterval(function() {
    const now = new Date().getTime();
    const distance = targetDate - now;

    if (distance < 0) {
      clearInterval(countdown);
      document.getElementById("countdown").innerHTML = "ゲーム開始！";
      return;
    }

    const days = Math.floor(distance / (1000 * 60 * 60 * 24));
    const hours = Math.floor((distance % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
    const seconds = Math.floor((distance % (1000 * 60)) / 1000);

    document.getElementById("countdown").innerHTML =
      `残り ${days}日 ${hours}時間 ${minutes}分 ${seconds}秒`;
  }, 1000);
</script>

</body>
</html>
